# Project URL only — no /rest/v1 path (the client adds that).
# Supabase Dashboard → Project Settings → API → Project URL
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
